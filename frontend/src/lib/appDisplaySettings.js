/**
 * When true, relationship labels (e.g. Son, Spouse) are hidden across the app.
 * Values may still exist in the database; they are simply not shown to users.
 */
export const HIDE_RELATION_NAMES_IN_UI = true;
