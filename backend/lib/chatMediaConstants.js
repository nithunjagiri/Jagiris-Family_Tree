const MAX_IMAGES_PER_SEND = 5;
const UPLOAD_MAX_BYTES = 600 * 1024;
const ALLOWED_CHAT_MIMES = ['image/jpeg', 'image/png', 'image/webp'];

module.exports = {
  MAX_IMAGES_PER_SEND,
  UPLOAD_MAX_BYTES,
  ALLOWED_CHAT_MIMES,
};
